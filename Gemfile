# frozen_string_literal: true

source 'https://rubygems.org'
gemspec

# Transient dependencies, not part of the final gem
# Used for the middleware classes
gem 'faraday', require: true
gem 'grape', require: true

gem 'rake'
gem 'rspec'
gem 'rubocop'
gem 'simplecov'
